<h1>New Person</h1>
<%= form_for :person, url: people_path do |f|%>

  <% if @person.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@person.errors.count, "error") %> prohibited
        this person's info from being saved:
      </h2>
      <ul>
        <% @person.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :favoritecity, "Favorite City" %><br>
    <%= f.text_field :favoritecity %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= form_for :person, url: people_path do |f| %>
  ...
<% end %>

<%= link_to 'Back', people_path %>